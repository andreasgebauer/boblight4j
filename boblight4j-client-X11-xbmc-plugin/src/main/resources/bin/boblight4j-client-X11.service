#!/bin/sh

BOBLIGHT_DIR=/storage/programs/clients
JAVA=/storage/programs/jre/bin/java

echo "Starting ${processName}"
$JAVA -jar $BOBLIGHT_DIR/${project.build.finalName}.jar -u 20 -x -o speed=70 -o interpolation=true -o threshold=30 -o saturation=1.5 -o value=10